[package]
name = "use-rust"
description = "Composable Rust ecosystem primitives for RustUse."
publish = true
authors.workspace = true
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
rust-version.workspace = true
readme = "README.md"
documentation = "https://docs.rs/use-rust"
keywords = ["rustuse", "rust", "crate", "version"]
categories = ["development-tools", "rust-patterns"]

[dependencies]
use-crate = { version = "0.1.1", path = "../use-crate" }
use-version = { version = "0.1.1", path = "../use-version" }
